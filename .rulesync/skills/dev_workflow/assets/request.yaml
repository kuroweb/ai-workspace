# Issue の要望を記録するファイル
# フェーズ 1 で AI が作成し、id / raw_input / created_at 等を埋める。

id: issue_XXX
# この Issue で扱うリポジトリ。config/projects.yaml の projects[].id。実装・PR は列挙したすべてで行う。
project_ids: []  # 例: ["ai-workspace"], ["my-app"] または ["api", "web"]
title: ""
created_at: ""  # ISO 8601 形式（例: 2026-02-14T10:30:00+09:00）
raw_input: |
  # ここにユーザーの要望をそのまま記載する（フェーズ 2 のヒアリングとビジネス要件の元になる）。
