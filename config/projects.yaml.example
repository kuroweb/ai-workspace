# 開発対象リポジトリの一覧
# request.yaml の project_ids で id を参照する
# 各フィールドの定義は .rulesync/skills/dev-workflow/references/flow.md を参照
#
# 注意: このファイルは環境固有の設定を含むため、
# 実行環境ごとに projects.yaml をコピーして編集してください

projects:
  - id: project-id
    name: "プロジェクト名"
    path: "~/path/to/project"
    repo: "github.com/username/repo"
    default_branch: main
    review_method: local_diff  # または pr
    notes: |
      - プロジェクトの説明
      - 複数行での記述も可能
